t=0:0.1:4*pi;
yn=0;
n = input('value of n: ');
for k=1:n
    yn=yn+(  cos((2*k-1)*t) * (4/((2*k-1)*pi))   );
end
plot(t,yn)
title('Fourier Series representation')
xlabel('F(t)')
legend('n=3','f(t)')